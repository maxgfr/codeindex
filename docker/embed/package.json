{
  "name": "codeindex-embed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "codeindex rich-tier embedding server: all-MiniLM-L6-v2 served over HTTP via transformers.js. NOT part of the zero-dependency engine — these deps belong to the image only.",
  "license": "MIT",
  "main": "server.mjs",
  "scripts": {
    "warm": "node warm.mjs",
    "start": "node server.mjs"
  },
  "dependencies": {
    "@xenova/transformers": "2.17.2",
    "onnxruntime-node": "1.14.0"
  }
}
