# quality-search fixture

A small service used to measure search relevance: authentication, rate
limiting, caching, pagination, migrations and webhooks each live in exactly one
file, so a query about a topic has one right answer.
